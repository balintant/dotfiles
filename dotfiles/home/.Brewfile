# Fallback only — these casks are declared here because mise's brew-cask
# backend cannot install them yet. Primary source of truth remains
# .config/mise/conf.d/packages-brew-cask.toml (each entry is commented there
# with the mise error). Remove from this file once mise supports them.
cask "arq"                # binary artifact resolution fails in mise
cask "blender"            # unsupported preflight_steps: set_permissions
cask "docker-desktop"     # unsupported postflight_steps: symlink
cask "paragon-ntfs"       # unsupported artifact type: installer
cask "parallels"          # xattr / preflight_steps failure in mise
cask "parallels-toolbox"  # unsupported artifact type: installer
