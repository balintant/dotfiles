set -g default-shell /bin/zsh
set -g default-command "exec /bin/zsh -l"
set -g destroy-unattached on

# ctrl-b is the default Tmux prefix

# use ctrl-b + n to create a new window
bind n new-window

# -n stands for "no prefix", meaning you don't have to
# press the prefix key before using the binding
bind -n C-n next-window
bind -n C-p previous-window

# use ctrl-b + c instead of ctrl-b + [ to enter the copy mode
bind c copy-mode

# use v instead of Space to begin selection, like in vim
bind -T copy-mode-vi v send -X begin-selection

# use y instead of Enter to copy selection, like in vim
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"

# Use vi keybindings in copy mode
setw -g mode-keys vi

# Address vim mode switching delay
set -s escape-time 10

# enable mouse
set -g mouse on

set-option -g status-justify centre
set-option -g status-keys vi
set-option -g status-left ''
set-option -g status-right ''
set-window-option -g window-status-format ' #W '

set-window-option -g window-status-current-format '#[bg=colour233] #[bold]#W#[nobold] '
set-option -g status-style fg=colour255,bg=colour235

# set-window-option -g window-status-current-format '#[bg=colour253] #[bold]#W#[nobold] '
# set-option -g status-style fg=colour235,bg=colour255
